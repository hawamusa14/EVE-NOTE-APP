# Server-side only — set these in Vercel/Netlify (or a local .env for `vercel dev` / `netlify dev`).
# Never put this key in index.html, script.js, or ai.js.

OPENAI_API_KEY=sk-your-key-here
# Optional model override (defaults to gpt-4o-mini):
# OPENAI_MODEL=gpt-4o-mini
